/**
 * Active locale. Components import `strings` from here; swapping the active
 * catalog (e.g. to an English one) happens in this single place.
 */
export { ru as strings } from "./ru";
export type { Strings } from "./ru";
